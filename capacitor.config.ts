import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fourtc.app',
  appName: 'four',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
